---
import Layout from '../../layouts/Layout.astro';
import FormInput from '../../components/FormInput.astro';
import BackgroundInputs from '../../components/BackgroundInputs.astro';
import GridInputs from '../../components/GridInputs.astro';
import ImportUrl from '../../components/ImportUrl.astro';
---

<Layout title="Generate TMT URL - Neuro Exercises">
    <main class="min-h-screen bg-background py-12 px-4 sm:px-6 lg:px-8">
        <div class="min-h-screen mx-auto max-w-4xl">
            <h1 class="text-4xl font-bold text-foreground mb-2">Generate TMT URL</h1>
            <p class="text-muted-foreground mb-8">Configure your Trail Making Test and generate a shareable URL</p>

            <div class="bg-card border border-border rounded-lg p-6 mb-8">
                <form id="urlForm" class="space-y-6">
                    <!-- Basic Settings -->
                    <div class="">
                        <h3 class="text-lg font-semibold text-foreground mb-4">Basic Settings</h3>

                        <FormInput
                                id="trailLengthInput"
                                label="Trail Length"
                                type="number"
                                value="10"
                                min="4"
                                max="20"
                                note="Total number of items in the trail"
                                className="mb-4"
                        />

                        <FormInput
                                id="numberRadiusInput"
                                label="Circle Radius (pixels)"
                                type="number"
                                value="30"
                                min="20"
                                max="50"
                                note="Size of the clickable circles"
                                className="mb-4"
                        />

                        <FormInput
                                id="textSizeInput"
                                label="Text Size (pixels)"
                                type="number"
                                value="18"
                                min="8"
                                max="96"
                                note="Size of the text inside circles"
                                className="mb-4"
                        />

                        <div class="mb-4">
                            <label class="block text-sm font-medium text-foreground mb-2">
                                Symbol Type
                            </label>
                            <select
                                    id="symbolTypeSelect"
                                    class="w-full px-4 py-2 bg-secondary border border-border rounded-lg text-foreground focus:outline-none focus:ring-2 focus:ring-primary"
                            >
                                <option value="numbers">Numbers Only (1-2-3-4...)</option>
                                <option value="letters">Letters Only (A-B-C-D...)</option>
                                <option value="numbers-letters">Numbers & Letters (1-A-2-B-3-C...)</option>
                                <option value="letters-numbers">Letters & Numbers (A-1-B-2-C-3...)</option>
                            </select>
                        </div>

                        <div class="mb-4">
                            <label class="flex items-center gap-2">
                                <input type="checkbox" id="reverseOrderCheck" class="text-primary" />
                                <span class="text-foreground font-medium">Reverse Order</span>
                            </label>
                            <p class="text-sm text-muted-foreground mt-1 ml-6">
                                When checked, symbols will appear in reverse order (e.g., 10-9-8... or Z-Y-X...)
                            </p>
                        </div>

                        <div class="mb-4">
                            <label class="flex items-center gap-2">
                                <input type="checkbox" id="showTimerCheck" checked class="text-primary" />
                                <span class="text-foreground font-medium">Show Timer</span>
                            </label>
                        </div>
                    </div>

                    <hr class="my-4 h-0.5 bg-border" />

                    <!-- Add this section after the Background Settings -->
                    <div class="">
                        <h3 class="text-lg font-semibold text-foreground mb-4">Advanced Settings</h3>

                        <div class="space-y-4">
                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="linesUnderDotsCheck" checked class="text-primary" />
                                    <span class="text-foreground font-medium">Lines Under Dots</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When checked, connection lines appear behind dots. When unchecked, lines appear over dots.
                                </p>
                            </div>

                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="allowWrongSelectionsCheck" checked class="text-primary" />
                                    <span class="text-foreground font-medium">Allow Wrong Selections</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When checked, wrong selections are recorded but the test continues. When unchecked, wrong selections are counted as errors but not selected.
                                </p>
                            </div>

                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="showWrongSelectionsCheck" checked class="text-primary" />
                                    <span class="text-foreground font-medium">Show Wrong Selections</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When true, wrong selections are visually shown as red in canvas. When false, wrong selections are visually shown as correct (still counts wrong in results data) in canvas.
                                </p>
                            </div>

                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="hidePopupButtonsCheck" class="text-primary" />
                                    <span class="text-foreground font-medium">Hide Popup Buttons</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When checked, the restart and menu buttons in the results popup are hidden.
                                </p>
                            </div>

                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="hidePopupResultsCheck" class="text-primary" />
                                    <span class="text-foreground font-medium">Hide Popup Results</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When checked, the time and errors display in the results popup are hidden.
                                </p>
                            </div>

                            <div>
                                <label class="flex items-center gap-2">
                                    <input type="checkbox" id="hidePopupAllCheck" class="text-primary" />
                                    <span class="text-foreground font-medium">Hide Popup Completely</span>
                                </label>
                                <p class="text-sm text-muted-foreground mt-1 ml-6">
                                    When checked, the results popup will not be shown at all after test completion.
                                </p>
                            </div>
                        </div>
                    </div>

                    <hr class="my-4 h-0.5 bg-border" />

                    <!-- Reusable Background Controls -->
                    <BackgroundInputs />

                    <hr class="my-4 h-0.5 bg-border" />

                    <!-- Reusable Grid Controls -->
                    <GridInputs />

                    <hr class="my-4 h-0.5 bg-border" />

                    <!-- Custom Positions Settings -->
                    <div>
                        <h3 class="text-lg font-semibold text-foreground mb-4">Custom Dot Positions</h3>

                        <div class="mb-4">
                            <label for="customPositionsInput" class="block text-sm font-medium text-foreground mb-2">
                                Custom Positions (JSON)
                            </label>
                            <textarea
                                    id="customPositionsInput"
                                    placeholder='[{"x": 10.5, "y": 20.75}, {"x": 30.25, "y": 40.8}, ...]'
                                    class="w-full h-32 px-4 py-2 bg-secondary border border-border rounded-lg text-foreground focus:outline-none focus:ring-2 focus:ring-primary font-mono text-sm"
                            ></textarea>
                            <p class="text-sm text-muted-foreground mt-2">
                                Optional: Provide exact positions as percentage coordinates (0.00-100.00).
                                Decimal values are supported for precise positioning.
                                Example: <code class="bg-secondary px-1 rounded">{"[{\"x\": 10.5, \"y\": 20.75}, {\"x\": 30, \"y\": 40}]"}</code>
                                where x: 10.5 means 10.5% from left, y: 20.75 means 20.75% from top.
                            </p>
                        </div>

                        <div class="mb-4">
                            <button
                                    type="button"
                                    id="generateRandomPositionsBtn"
                                    class="px-4 py-2 bg-primary text-primary-foreground rounded-lg font-medium hover:opacity-90 transition-opacity mb-2"
                            >
                                Generate Random Positions
                            </button>
                            <p class="text-sm text-muted-foreground">
                                Automatically generates random positions for the current trail length.
                            </p>
                        </div>
                    </div>
                </form>
            </div>

            <!-- Add this after the form and before the Generated URL section -->
            <ImportUrl exercise="TMT" />

            <!-- Generated URL -->
            <div id="resultSection" class="bg-card border border-border rounded-lg p-6">
                <h2 class="text-xl font-bold text-foreground mb-4">Generated URL</h2>

                <div class="bg-secondary rounded-lg p-4 mb-4 overflow-x-auto">
                    <code id="generatedUrl" class="text-sm text-foreground break-all"></code>
                </div>

                <div class="flex gap-4">
                    <button
                            id="copyBtn"
                            class="px-6 py-2 bg-primary text-primary-foreground rounded-lg font-medium hover:opacity-90 transition-opacity"
                    >
                        Copy URL
                    </button>
                    <button
                            id="openBtn"
                            class="px-6 py-2 bg-secondary text-secondary-foreground rounded-lg font-medium hover:bg-accent transition-colors"
                    >
                        Open TMT
                    </button>
                </div>
            </div>
        </div>
    </main>

    <script>
        import { WebviewWindow } from "@tauri-apps/api/webviewWindow";

        async function safeIsTauri() {
            try {
                const { isTauri } = await import('@tauri-apps/api/core');
                return isTauri();
            } catch {
                return false; // Browser
            }
        }
        const windowTauri = await safeIsTauri();

        let currentPlatform = "web";
        if (windowTauri) {
            const os = await import("@tauri-apps/plugin-os");
            currentPlatform = os.platform();
        }

        const openBtn = document.getElementById('openBtn');
        const generatedUrlEl = document.getElementById('generatedUrl');

        openBtn.addEventListener("click", (e) => {
            e.preventDefault();

            const url = generatedUrlEl.textContent.trim();
            if (!url) return;

            if (windowTauri && currentPlatform !== "android" && currentPlatform !== "ios") {
                // Generate a unique window label
                const label = `tmt-${Date.now()}`;

                // Create the new window
                const win = new WebviewWindow(label, {
                    url
                });

                // Optional: handle events
                win.once("tauri://created", () => {
                    console.log("TMT window created");
                });
                win.once("tauri://error", (e) => {
                    console.error("Window creation error:", e);
                });
            } else {
                // Running in browser - open in new tab
                window.open(url, '_blank', 'noopener,noreferrer');
            }
        });
    </script>
    <script is:inline src={`${process.env.GITHUB_PAGES ? import.meta.env.BASE_URL : ""}/scripts/TMTGenerateScript.js`}></script>
</Layout>